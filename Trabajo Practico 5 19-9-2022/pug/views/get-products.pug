




doctype html
html
    include includes/head.pug
    body 
        if products.length
            table.table
                thead
                    tr(style='border: 1px solid black')
                    th(scope='col')(style='border: 1px solid black') Nombre Prodcuto
                    th(scope='col')(style='border: 1px solid black') Precio
                    th(scope='col')(style='border: 1px solid black') Imagen
                tbody.table-group-divider
                    each item in products
                        tr
                            td(style='border: 1px solid black')= item.title
                            td(style='border: 1px solid black')= item.price
                            td(style='border: 1px solid black')
                                img(src=item.thumbnail, width="50")
        else
            p No Hay Prodcutos
        a.btn.btn-success(href='/') Crear Producto
        include includes/foot.pug